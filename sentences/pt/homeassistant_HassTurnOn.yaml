language: pt
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<liga> [a[s]|o[s]] [luz[es]|interruptor[es]|lâmpada[s]] [do|da|de] <nome>"
          - "<liga> <nome>"
        excludes_context:
          domain:
            - binary_sensor
            - cover
            - lock
            - scene
            - script
            - sensor
